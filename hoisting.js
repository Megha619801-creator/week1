// 1.
function cube(x) {
  return x * x * x;
}

const cube = function(x){
     return x*x*x;
}
   

// 2.
function fullName(first, last) {
  return first + " " + last;
}

const fullName = function(first, last){
    return first + " " + last;
}

